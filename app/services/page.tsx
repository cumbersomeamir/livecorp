import ServicesHangar from '@/components/services/ServicesHangar';

export const metadata = {
  title: 'Services — Live Corp',
  description: 'Pre-diagnostic intelligence systems designed to reduce unnecessary tests safely.',
};

export default function ServicesPage() {
  return <ServicesHangar />;
}

