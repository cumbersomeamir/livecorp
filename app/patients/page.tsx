import PatientsPage from '@/components/pages/PatientsPage';

export const metadata = {
  title: 'For Patients — Live Corp',
  description: 'What users do at home, what outputs mean, red flags, and limitations.',
};

export default function Patients() {
  return <PatientsPage />;
}

