import ContactPage from '@/components/pages/ContactPage';

export const metadata = {
  title: 'Contact — Live Corp',
  description: 'Get in touch with Live Corp.',
};

export default function Contact() {
  return <ContactPage />;
}

